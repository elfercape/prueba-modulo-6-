<nav class="navbar navbar-expand-lg bg-body-secondary">
  <div class="container-fluid">
    <a class="navbar-brand text-success" href="#">Plataforma Interna</a>
    <% if user_signed_in? %>
       <b> <%= current_user.email %></b>
    <% end%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      
      <div class="d-flex mx-2 justify-content-end align-items-center">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          
          <% if user_signed_in? %>
           Hola <%= current_user.email %>
           <% else %>
            Dale registrate o conectate
          <% end %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <% if user_signed_in? %>
              <li><%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
            <% else %>
              <li><a class="dropdown-item text-danger" href="/users/sign_in">Inicio Sesion</a></li>
              <li><a class="dropdown-item text-danger" href="/users/sign_up">Registro</a></li>
             <% end %>
          </ul>
        </li>
      </div>
    </div>
  </div>
</nav>